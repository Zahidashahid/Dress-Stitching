// DropZone.cs
using UnityEngine;

public class DropZone : MonoBehaviour
{
    public int acceptedclothPieceNum;
    /*private void OnTriggerEnter2D(Collider2D collision)
    {
        ClothPiece clothPiece = collision.GetComponent<ClothPiece>();

        if (clothPiece != null )
        {
            // Adjust the position of the cloth piece to the center of the drop zone
            clothPiece.transform.position = transform.position;
            clothPiece.setToDropZone = true;
        }
    }*/
}
